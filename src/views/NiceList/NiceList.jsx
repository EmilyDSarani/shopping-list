import React from 'react'
//if logic is related to the same piece of state, then it all goes together
//add, delete, change is all in the same state
//so it is one state
export default function NiceList() {
    return (
        <div>
            
        </div>
    )
}
